namespace IntervalTimerApp.Models
{
    /// <summary>
    /// Timer Status
    /// </summary>
    public enum TimerStatus
    {
        // Runned
        Start,
        // Stopped
        Stop
    }
}